# Frontend dev-mode env vars. Loaded by Vite when mode=development
# (`pnpm -C packages/frontend dev`, i.e. `pnpm dev:web`).
# The wrapper-native dev server listens on :3001 and mounts the API at /api/v1.
VITE_API_BASE_URL=http://localhost:3001/api/v1
VITE_APP_NAME=MercadoExpress
