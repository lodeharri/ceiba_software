/**
 * Lazy-loaded JetBrains Mono — imported only in components that display SKU/mono data.
 * Vite will create a separate CSS chunk for this import, keeping it out of the critical path.
 */
import '@fontsource-variable/jetbrains-mono';
